<h1>Show</h1>

<div>
  <h1><%= @team.name %></h1>
</div>

<div>
  <p><%= link_to("Edit Team", edit_team_path(@team)) %></p>
  <p>  <%= link_to "Delete team", team_path(@team), class: "button", method: :delete, 
                                            data: {confirm: 'Are you sure?'} %>
</p>
</div>